


class renderUtils {
    constructor(options) {

    }

    //渲染point
    point(pointList,d,z) { 
        console.log(pointList, d, z)
        return pointList
    }
    nav(nextDir, d, z) {
        console.log(nextDir, d, z)
        return 
     }
    focus(focusList, d, z) { 
        console.log(focusList, d, z)
        return focusList
    }
    center(z) { 
        console.log( z)
        return 
    }

}

module.exports = renderUtils
